\section{Discussion}
\label{sec:discussion}

\subsection{Theoretical Implications}

Our findings reveal a \textbf{fundamental tension} in actor-critic design for continuous control:

\begin{enumerate}
    \item \textbf{Bounded outputs are necessary} for valid action generation in continuous spaces
    \item \textbf{Bounded activations (tanh) are susceptible} to saturation and gradient vanishing
    \item \textbf{Large reward scales and high learning rates} exacerbate saturation
    \item \textbf{Critics with linear outputs} are immune to this specific pathology
\end{enumerate}

This creates an inherent asymmetry: critics can use aggressive learning rates, while actors require conservative rates to avoid saturation.

\subsection{The Learning Rate Paradox}

The $10^6\times$ gradient asymmetry creates a paradox where \textbf{no actor learning rate is optimal}:

\textbf{Low Actor LR ($\eta_a \leq 0.0001$):}
\begin{itemize}
    \item Actor updates are too small relative to critic's rapid Q-landscape changes
    \item The actor ``chases a moving target'' -- by the time it adapts to the current Q-function, the critic has already shifted
    \item Result: Slow convergence, suboptimal policies
\end{itemize}

\textbf{High Actor LR ($\eta_a \geq 0.01$):}
\begin{itemize}
    \item Large weight updates push pre-activation values into saturation regions
    \item tanh outputs lock at $\pm 1$, gradients vanish
    \item Result: Actor stops learning entirely within a few episodes
\end{itemize}

\textbf{The ``Chasing a Moving Target'' Dynamic:}

The critic learns $10^6\times$ faster than the actor due to gradient magnitude differences. This creates a non-stationary optimization landscape for the actor:

\begin{equation}
\frac{\partial Q}{\partial \theta_c} \gg \frac{\partial \mathcal{L}_a}{\partial \theta_a} \implies \Delta \theta_c \gg \Delta \theta_a
\end{equation}

The actor's policy $\pi_{\theta_a}$ is optimized against $Q_{\theta_c}$, but $\theta_c$ changes substantially between actor updates. The actor effectively optimizes against a stale Q-function, leading to:
\begin{enumerate}
    \item Oscillating policies that never stabilize
    \item Suboptimal convergence to local minima
    \item Increased sensitivity to learning rate selection
\end{enumerate}

This explains why the recommended actor/critic LR ratio of $\sim$0.1 (actor LR $<$ critic LR) works empirically -- it partially compensates for the gradient magnitude imbalance, though it cannot fully resolve the non-stationarity issue.

\subsection{Convergence Rate as the Critical Metric}

A key distinction must be made between \textit{preventing stopping} (maintaining gradient flow) and \textit{achieving convergence} (producing policies with competitive reward). Preventing stopping is necessary but not sufficient: an actor that continues updating but never converges to a good policy has not benefited from the mitigation.

\subsubsection{Prior Evidence: 1 of 16 Configurations Converge (Thesis)}

The severity of the learning rate sensitivity was first established in our original thesis experiments~\cite{gebrekidan2024thesis}, which used a \textit{different random seed and environment configuration} from the current work. Those experiments conducted 10 independent runs per configuration with 95\% confidence intervals, finding that \textbf{only 1 of 16 learning rate combinations consistently achieved policy improvement}: actor LR = 0.0001 with critic LR = 0.001. This optimal configuration falls precisely within the narrow window predicted by our analysis:
\begin{itemize}
    \item Actor LR = 0.0001 is conservative enough to avoid saturation (pre-activations remain bounded)
    \item Critic LR = 0.001 provides sufficient learning speed while maintaining stability
    \item The ratio $\eta_a / \eta_c = 0.1$ compensates for the gradient magnitude asymmetry
\end{itemize}

All 15 other thesis configurations exhibited either:
\begin{enumerate}
    \item \textbf{Premature stopping} (actor LR $\geq$ 0.01): Tanh saturation within a few episodes
    \item \textbf{High variance} (actor LR = 0.0001, critic LR = 0.0001): Too slow to converge reliably within the training horizon
    \item \textbf{Instability} (actor LR = 0.001, critic LR = 0.1): Critic changes too rapidly for actor to track
\end{enumerate}

\subsubsection{Current Experiments: Consistent Structural Patterns}

Our current experiments (seed = 42, GPU-optimized implementation with updated MEC scaling) show the same structural patterns---actor LR is the dominant factor in stopping, and high actor learning rates cause stopping within 161--260 episodes. However, due to the different seed and environment configuration, the convergence rates are not directly comparable to the thesis results. The current single-run experiments serve to identify the \textit{mechanism} behind stopping (tanh saturation), not to establish convergence statistics.

\subsubsection{Implications for Hyperparameter Search}

Whether 1 of 16 (thesis) or a similarly small fraction converges, the practical barrier is the same: the vast majority of hyperparameter configurations fail because actors stop updating due to activation saturation. This has profound implications for computational costs: exhaustive hyperparameter search wastes resources on configurations that are fundamentally incapable of learning.

However, \textit{simply preventing stopping does not guarantee convergence}. As our mitigation experiments demonstrate (Section~\ref{sec:discussion}\ref{subsec:mitigation_summary}), a mitigation that prevents stopping in 16/16 configurations but degrades reward in 13/16 (InputNorm) is worse than one that prevents stopping in 12/16 but improves reward in 9/16 (adaptive scaling). The convergence rate---defined as the number of configurations achieving competitive reward---is the appropriate metric for evaluating mitigations, not the stopping rate alone.

\subsection{Practical Recommendations}

Based on our analysis, we recommend:

\textbf{Learning Rate Selection:}
\begin{itemize}
    \item Actor learning rate: 0.0001 - 0.001 (conservative)
    \item Critic learning rate: 0.001 - 0.01 (can be 10$\times$ higher than actor)
    \item Recommended ratio: Actor LR / Critic LR $\approx$ 0.1
\end{itemize}

\textbf{Reward Scaling:}
\begin{itemize}
    \item Normalize rewards to unit scale when possible
    \item Monitor effective learning rate = $\eta \times |R|$
    \item Keep effective LR $<$ 0.1 for actors
\end{itemize}

\textbf{Architecture Modifications:}
\begin{itemize}
    \item Consider replacing tanh with scaled sigmoid or softsign
    \item Add gradient clipping on critic updates (see Figure~\ref{fig:gradient_clipping} for why actor clipping is ineffective)
    \item Monitor pre-activation magnitudes during training
\end{itemize}

\textbf{Training Monitoring:}
\begin{itemize}
    \item Implement weight change detection for actors
    \item Track tanh output distribution (warning if clustered at $\pm 1$)
    \item Compare actor vs critic gradient magnitudes
\end{itemize}

\subsection{Analysis from Multiple Perspectives}

To ensure comprehensive understanding, we analyze our findings from multiple theoretical perspectives.

\subsubsection{Optimization Perspective}

From an optimization standpoint, the actor and critic are jointly solving a bilevel optimization problem where the actor optimizes policy performance given the critic's value estimates, while the critic learns to accurately predict returns. The gradient asymmetry we observe implies that this joint optimization is inherently unbalanced---the critic's optimization landscape changes faster than the actor can track, violating the quasi-static assumption that underlies many convergence proofs for actor-critic methods~\cite{konda2000actor}.

\subsubsection{Information-Theoretic Perspective}

From an information flow perspective, the critic receives direct reward feedback (high information bandwidth), while the actor receives reward information only indirectly through the critic's Q-value gradients (low information bandwidth). The tanh saturation further reduces this bandwidth by compressing the gradient signal. This information bottleneck explains why actors are more sensitive to hyperparameter choices---they operate with less margin for error in extracting learning signal from their limited information channel.

\subsubsection{Stability Analysis Perspective}

The differential convergence behavior can be viewed through the lens of dynamical systems stability. The actor-critic system has two coupled dynamical subsystems with different time constants. When the actor's effective time constant (inverse learning rate) is too small relative to the critic's, the system enters an unstable regime where the actor overshoots, saturates, and loses the ability to track the critic's changes. Conservative actor learning rates increase the actor's time constant, improving stability at the cost of slower adaptation.

\subsubsection{Robustness of CCM-MADRL Despite Actor Freezing}

An important observation is that CCM-MADRL continues to improve performance even after actors stop updating. This robustness stems from the master agent's selection mechanism: even with fixed actor policies, the critic continues refining Q-value estimates, enabling better selection of which agents to include in the offloading coalition. This architectural feature provides partial immunity to actor stagnation but does not eliminate the underlying gradient pathology.

\subsection{Broader Impact}

This analysis has implications beyond MEC task offloading:

\begin{enumerate}
    \item \textbf{DDPG/TD3/SAC implementations:} All use tanh-bounded outputs and may exhibit similar behavior. TD3's delayed policy updates~\cite{fujimoto2018addressing} may partially mitigate this by reducing actor update frequency, implicitly increasing the effective actor time constant.

    \item \textbf{Hyperparameter transfer:} Learning rates optimal in one domain may fail in others with different reward scales. Our analysis suggests that practitioners should normalize rewards or adjust learning rates proportionally when transferring hyperparameters across domains.

    \item \textbf{Multi-agent systems:} Shared rewards amplify the effective reward scale ($N$ agents $\times$ per-agent contribution), making MARL systems particularly susceptible to the saturation effects we characterize.

    \item \textbf{Reward shaping:} Dense reward shaping, often used to accelerate learning, can inadvertently increase reward magnitude and trigger premature actor saturation if learning rates are not correspondingly reduced.
\end{enumerate}

\subsection{Mitigation Strategies from the Literature}

Several techniques in the literature address aspects of the gradient asymmetry and saturation problems we identify. We categorize these by their mechanism of action.

\subsubsection{Implicit Learning Rate Adjustment}

\begin{itemize}
    \item \textbf{Delayed policy updates (TD3)~\cite{fujimoto2018addressing}:} By updating the actor less frequently than the critic (typically every 2 critic updates), TD3 effectively halves the actor's learning rate relative to the critic. This aligns with our recommendation for conservative actor learning rates and partially compensates for gradient magnitude asymmetry.

    \item \textbf{Target networks~\cite{lillicrap2015continuous}:} Target networks stabilize critic learning by providing slowly-moving Q-targets, reducing the non-stationarity that actors must track. However, this does not directly address actor saturation.
\end{itemize}

\subsubsection{Alternative Action Parameterizations}

These approaches avoid tanh saturation by using different output distributions:

\begin{itemize}
    \item \textbf{Squashed Gaussian (SAC)~\cite{haarnoja2018soft}:} SAC parameterizes the policy as a Gaussian distribution over unbounded actions, then applies tanh squashing \textit{after} sampling. Crucially, gradients flow through the unbounded mean and standard deviation parameters, not through the tanh function. The log-probability correction $\log \pi(a|s) = \log \mu(u|s) - \sum_i \log(1 - \tanh^2(u_i))$ accounts for the squashing without creating gradient bottlenecks. This is the most direct solution to tanh saturation in the literature.

    \item \textbf{Beta distribution~\cite{chou2017improving}:} Instead of squashing unbounded outputs, this approach uses the Beta distribution which naturally produces bounded outputs in $[0, 1]$. The policy outputs shape parameters $\alpha, \beta > 0$, and actions are sampled directly from $\text{Beta}(\alpha, \beta)$. This eliminates the saturating activation entirely while maintaining valid bounded actions.
\end{itemize}

\subsubsection{Normalization Techniques}

\begin{itemize}
    \item \textbf{Reward normalization~\cite{henderson2018deep}:} Normalizing rewards to zero mean and unit variance reduces the effective learning rate ($\eta \times |R|$), preventing the large weight updates that cause saturation. This is particularly important in multi-agent settings where reward magnitudes scale with agent count.

    \item \textbf{Layer normalization~\cite{ba2016layer}:} Applying layer normalization before the tanh output keeps pre-activation values bounded, preventing extreme inputs that cause saturation. Unlike batch normalization~\cite{ioffe2015batch}, layer normalization operates per-sample and is suitable for RL where batch statistics are non-stationary.
\end{itemize}

\subsubsection{Training Procedure Modifications}

\begin{itemize}
    \item \textbf{Phasic policy gradient (PPG)~\cite{cobbe2021phasic}:} PPG separates policy and value function training into distinct phases, reducing gradient interference between actor and critic. This addresses the ``chasing a moving target'' dynamic by allowing the critic to stabilize before actor updates.

    \item \textbf{Entropy regularization (SAC)~\cite{haarnoja2018soft}:} The entropy bonus $\mathcal{H}[\pi(\cdot|s)]$ encourages policy diversity, potentially preventing early convergence to deterministic (saturated) outputs. However, this is an indirect mitigation that does not address gradient magnitude asymmetry.

    \item \textbf{Gradient clipping~\cite{pascanu2013difficulty}:} While gradient clipping can prevent exploding gradients in critics, it does not address the \textit{vanishing} gradient problem in actors caused by tanh saturation---clipping reduces large gradients but cannot amplify small ones.
\end{itemize}

\subsubsection{Summary and Recommendations}

Table~\ref{tab:mitigations} summarizes the mitigation strategies and their effectiveness against our identified causes.

\begin{table}[htbp]
\caption{Mitigation Strategies vs. Causes of Gradient Asymmetry}
\label{tab:mitigations}
\centering
\small
\begin{tabular}{lccc}
\toprule
\textbf{Mitigation} & \textbf{Loss} & \textbf{Path} & \textbf{Activation} \\
 & \textbf{Asymm.} & \textbf{Length} & \textbf{Saturation} \\
\midrule
Delayed updates (TD3) & \ding{51} & -- & -- \\
Squashed Gaussian (SAC) & -- & -- & \ding{51}\ding{51} \\
Beta distribution & -- & -- & \ding{51}\ding{51} \\
Reward normalization & \ding{51} & -- & \ding{51} \\
Layer normalization & -- & -- & \ding{51} \\
Phasic training (PPG) & -- & \ding{51} & -- \\
Entropy regularization & -- & -- & \ding{51} \\
\bottomrule
\multicolumn{4}{l}{\footnotesize \ding{51}\ding{51} = directly addresses; \ding{51} = partially addresses; -- = does not address}
\end{tabular}
\end{table}

Based on our analysis, the most effective mitigations for tanh saturation are \textbf{Squashed Gaussian} and \textbf{Beta distribution} parameterizations, as they bypass the saturating activation entirely. For practitioners using deterministic policies (DDPG/TD3), combining \textbf{reward normalization} with \textbf{layer normalization} before the tanh output provides a practical alternative without changing the policy structure.

\subsection{Summary of Evidence}

Our comprehensive experiments validate the theoretical analysis through multiple lines of evidence:

\begin{enumerate}
    \item \textbf{Gradient asymmetry:} Measured ratio $\rho \sim 10^{-8}$ to $10^{-4}$ confirms 4--8 orders of magnitude critic gradient dominance across all configurations.

    \item \textbf{Saturation correlation:} Strong correlation ($r > 0.9$) between final saturation ratio and stopping episode demonstrates the causal chain from high LR to saturation to weight update cessation.

    \item \textbf{Layer-wise analysis:} Gradient breakdown occurs specifically at the actor output layer (tanh), while hidden layers (ReLU) maintain gradient flow.

    \item \textbf{Consistency:} The pattern holds across all 16 learning rate configurations, with actor LR as the dominant factor and critic LR having secondary effect only at extreme values (0.1).
\end{enumerate}

These findings provide both theoretical explanation and empirical validation for the gradient asymmetry phenomenon in actor-critic architectures.

\subsection{Network Capacity Does Not Prevent Saturation}

A natural hypothesis is that increasing actor network capacity might prevent saturation by distributing the learning signal across more parameters and producing smaller pre-activation values. To test this, we conducted an ablation study where the actor hidden layers were increased from $(64 \rightarrow 32)$ to $(512 \rightarrow 128)$---matching the critic's hidden layer dimensions.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.9\columnwidth]{figures/large_actor_comparison.pdf}
    \caption{Comparison of stopping episodes between small actor (64$\rightarrow$32, $\sim$2,500 parameters) and large actor (512$\rightarrow$128, $\sim$70,000 parameters). Despite 28$\times$ more parameters, the large actor still exhibits early stopping due to saturation. The green dashed line indicates the maximum training duration (2,000 episodes).}
    \label{fig:large_actor}
\end{figure}

Table~\ref{tab:large_actor} summarizes the results across all 16 learning rate configurations.

\begin{table}[htbp]
\caption{Stopping Episodes: Small vs Large Actor Architecture}
\label{tab:large_actor}
\centering
\small
\begin{tabular}{cccc}
\toprule
\textbf{Actor LR} & \textbf{Critic LR} & \textbf{Small (64$\rightarrow$32)} & \textbf{Large (512$\rightarrow$128)} \\
\midrule
0.1 & 0.1 & 161 & 161 \\
0.1 & 0.01 & 162 & 208 \\
0.1 & 0.001 & 222 & 246 \\
0.1 & 0.0001 & 213 & 231 \\
0.01 & 0.1 & 260 & 260 \\
0.01 & 0.01 & 253 & 246 \\
0.01 & 0.001 & 259 & 267 \\
0.01 & 0.0001 & 260 & 266 \\
0.001 & 0.1 & 944 & 298 \\
0.001 & 0.01 & 2000 & 278 \\
0.001 & 0.001 & 2000 & 284 \\
0.001 & 0.0001 & 2000 & 271 \\
0.0001 & 0.1 & 658 & 1078 \\
0.0001 & 0.01 & 2000 & 2000 \\
0.0001 & 0.001 & 2000 & 2000 \\
0.0001 & 0.0001 & 2000 & 2000 \\
\bottomrule
\end{tabular}
\end{table}

The results demonstrate that \textbf{increasing actor network capacity does not prevent tanh saturation}---and in fact \textbf{worsens it}. The large actor achieves only \textbf{3 of 16} no-stop configurations compared to \textbf{6 of 16} in the baseline. Most strikingly, three configurations that completed full training with the small actor (actor LR = 0.001, critic LR $\leq$ 0.01) now stop between episodes 271--298 with the large actor. Even the surviving configurations at actor LR = 0.0001 show partial actor stopping (5--13 of 50 agents).

For high actor learning rates ($\geq 0.01$), stopping episodes remain comparable between architectures---both stop well before 300 episodes. The large actor shows marginal differences of $\pm$20 episodes, confirming that saturation onset is governed by the learning rate rather than network size.

This finding confirms that the saturation problem is \textit{architectural} rather than \textit{capacity-limited}:
\begin{itemize}
    \item The bottleneck is the \textbf{tanh output activation}, not the hidden layer capacity
    \item Larger networks produce extreme pre-activation values that saturate tanh \textit{faster}, as more parameters enable larger weight updates per gradient step
    \item The 28$\times$ increase in parameters (from $\sim$2,500 to $\sim$70,000) shifts the viable operating region to \textit{lower} learning rates rather than expanding it
\end{itemize}

This result reinforces our recommendation that effective mitigations must address the output activation itself (e.g., Squashed Gaussian, Beta distribution) rather than simply increasing network size. Indeed, increasing capacity is counterproductive: it accelerates the very pathology it was hypothesized to prevent.

\subsection{Linear Hidden Activations Exacerbate the Problem (Preliminary)}

\textit{Note: The following experiments are ongoing. Results presented here are preliminary and subject to revision.}

A natural hypothesis is that ReLU activations in hidden layers might contribute to saturation by producing only positive values, potentially misaligned with negative reward signals. To test this, we replaced ReLU with linear (identity) activations in both actor and critic hidden layers, allowing the network to naturally represent negative values throughout computation.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.9\columnwidth]{figures/linear_activation_comparison.pdf}
    \caption{Comparison of stopping episodes between ReLU and linear hidden layer activations. Despite removing nonlinearity from hidden layers, early stopping still occurs. The green dashed line indicates full training duration (2,000 episodes).}
    \label{fig:linear_activation}
\end{figure}

Table~\ref{tab:linear_activation} compares the results.

\begin{table}[htbp]
\caption{Stopping Episodes: ReLU vs Linear Hidden Activations}
\label{tab:linear_activation}
\centering
\small
\begin{tabular}{cccc}
\toprule
\textbf{Actor LR} & \textbf{Critic LR} & \textbf{ReLU Hidden} & \textbf{Linear Hidden} \\
\midrule
0.1 & 0.1 & 161 & 301 \\
0.1 & 0.01 & 176 & 251 \\
0.1 & 0.001 & 217 & 250 \\
0.1 & 0.0001 & 215 & 269 \\
0.01 & 0.1 & 247 & 307 \\
0.01 & 0.01 & 233 & 307 \\
0.01 & 0.001 & 233 & 286 \\
0.01 & 0.0001 & 233 & 276 \\
\bottomrule
\end{tabular}
\end{table}

Surprisingly, linear hidden activations show \textit{slightly delayed} stopping (33--140 additional episodes), but \textbf{pre-activation explosion is dramatically worse}:

\begin{table}[htbp]
\caption{Pre-activation Magnitudes: ReLU vs Linear Hidden Layers}
\label{tab:linear_preact}
\centering
\small
\begin{tabular}{ccc}
\toprule
\textbf{Actor LR} & \textbf{ReLU Pre-activation Range} & \textbf{Linear Pre-activation Range} \\
\midrule
0.1 & $\pm 1.07 \times 10^{6}$ & $\pm 1.83 \times 10^{7}$ \\
0.01 & $\pm 5.2 \times 10^{3}$ & $\pm 8.1 \times 10^{4}$ \\
\bottomrule
\end{tabular}
\end{table}

With linear hidden layers, pre-activations explode to \textbf{18 million} (vs 1 million with ReLU)---a 17$\times$ increase. This occurs because:
\begin{itemize}
    \item ReLU provides implicit regularization by clipping negative values
    \item Without nonlinearity, the network is effectively a single deep linear transformation
    \item Large weights propagate directly through all layers without any dampening
\end{itemize}

This experiment conclusively demonstrates that the saturation problem originates at the \textbf{tanh output layer}, not the hidden layer activations. Modifying hidden layers---whether by changing size or activation function---cannot address the fundamental issue of bounded output activations combined with large learning rates.

\subsection{Gradient Clipping Does Not Prevent Actor Saturation (Preliminary)}

\textit{Note: The following experiments are ongoing. Results presented here are preliminary and subject to revision.}

A commonly recommended practice in deep RL is gradient clipping~\cite{pascanu2013difficulty}, which limits gradient norms to prevent explosive updates. We tested whether applying gradient clipping (max\_grad\_norm = 1.0, a standard value used in PPO, TD3, etc.) to both actor and critic networks could prevent the saturation-induced actor stopping.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.9\columnwidth]{figures/gradient_clipping_comparison.pdf}
    \caption{Comparison of stopping episodes between baseline (no clipping) and gradient clipping (max\_grad\_norm=1.0). For high actor learning rates ($\geq 0.01$), gradient clipping provides no benefit. For low actor learning rates with high critic LR (0.1), clipping stabilizes training by preventing critic gradient explosion. The blue dashed line indicates full training duration (2,000 episodes).}
    \label{fig:gradient_clipping}
\end{figure}

Table~\ref{tab:gradient_clipping} summarizes the results for configurations where the two approaches differ meaningfully.

\begin{table}[htbp]
\caption{Stopping Episodes: Baseline vs Gradient Clipping}
\label{tab:gradient_clipping}
\centering
\small
\begin{tabular}{cccc}
\toprule
\textbf{Actor LR} & \textbf{Critic LR} & \textbf{Baseline} & \textbf{Grad Clipping} \\
\midrule
0.1 & 0.1 & 161 & 200 \\
0.1 & 0.01 & 162 & 197 \\
0.1 & 0.001 & 222 & 213 \\
0.1 & 0.0001 & 213 & 211 \\
0.01 & 0.1 & 260 & 247 \\
0.01 & 0.01 & 253 & 244 \\
0.01 & 0.001 & 259 & 244 \\
0.01 & 0.0001 & 260 & 253 \\
0.001 & 0.1 & 944 & \textbf{2000} \\
0.0001 & 0.1 & 658 & \textbf{2000} \\
\bottomrule
\end{tabular}
\end{table}

The results reveal a nuanced picture:

\begin{itemize}
    \item \textbf{High actor LR ($\geq 0.01$):} Gradient clipping provides \textit{no meaningful benefit}. Stopping episodes are nearly identical to baseline, confirming that the problem is vanishing (not exploding) actor gradients caused by tanh saturation.

    \item \textbf{Low actor LR with high critic LR (0.1):} Gradient clipping \textit{does help} in two configurations: actor LR 0.001 with critic LR 0.1 improved from 944 to 2000 episodes, and actor LR 0.0001 with critic LR 0.1 improved from 658 to 2000 episodes. In these cases, clipping prevents the \textit{critic's} explosive gradients from destabilizing training.

    \item \textbf{Overall:} 8 of 16 configurations reached 2000 episodes with clipping vs 6 of 16 without---a modest improvement.
\end{itemize}

This confirms our theoretical prediction: gradient clipping addresses \textit{exploding} gradients (critic side) but cannot amplify \textit{vanishing} gradients (actor side). The two configurations that improved were those where the critic LR was highest (0.1), causing critic gradient explosion that destabilized the actor indirectly. For the fundamental saturation problem at high actor learning rates, gradient clipping is ineffective.

\subsection{Layer Normalization Prevents Stopping but Degrades Learning (Preliminary)}

\textit{Note: The following experiments are ongoing. Results presented here are preliminary and subject to revision.}

A promising mitigation strategy is to apply layer normalization~\cite{ba2016layer} before the tanh output activation, keeping pre-activation values bounded and preventing the extreme inputs that cause saturation. We tested this by adding a LayerNorm layer immediately before the actor's tanh output across all 16 learning rate configurations.

\subsubsection{Stopping Behavior: Significant Improvement}

LayerNorm dramatically reduces actor stopping: \textbf{13 of 16 configurations} reached full training (2,000 episodes) compared to only 6 of 16 in the baseline. Table~\ref{tab:layernorm_stopping} compares stopping episodes.

\begin{table}[htbp]
\caption{Stopping Episodes: Baseline vs LayerNorm}
\label{tab:layernorm_stopping}
\centering
\small
\begin{tabular}{ccrr}
\toprule
\textbf{Actor LR} & \textbf{Critic LR} & \textbf{Baseline} & \textbf{LayerNorm} \\
\midrule
0.1 & 0.1 & 161 & 1069 \\
0.1 & 0.01 & 162 & \textbf{2000} \\
0.1 & 0.001 & 222 & \textbf{2000} \\
0.1 & 0.0001 & 213 & \textbf{2000} \\
0.01 & 0.1 & 260 & 1237 \\
0.01 & 0.01 & 253 & \textbf{2000} \\
0.01 & 0.001 & 259 & \textbf{2000} \\
0.01 & 0.0001 & 260 & \textbf{2000} \\
0.001 & 0.1 & 944 & 719 \\
0.001 & 0.01 & 2000 & 2000 \\
0.001 & 0.001 & 2000 & 2000 \\
0.001 & 0.0001 & 2000 & 2000 \\
0.0001 & 0.1 & 658 & \textbf{2000} \\
0.0001 & 0.01 & 2000 & 2000 \\
0.0001 & 0.001 & 2000 & 2000 \\
0.0001 & 0.0001 & 2000 & 2000 \\
\bottomrule
\end{tabular}
\end{table}

Seven configurations that previously stopped now run to completion (bold entries in Table~\ref{tab:layernorm_stopping}). The three remaining stopped configurations all have critic LR = 0.1. Two show substantial delays compared to baseline (e.g., actor LR 0.1 improved from 161 to 1,069 episodes), but one regresses: actor LR 0.001 with critic LR 0.1 stops at episode 719 with LayerNorm versus 944 without. This pattern indicates that LayerNorm effectively addresses actor-side saturation for high actor learning rates but cannot fully compensate when the critic's learning rate is an order of magnitude larger than any actor learning rate.

\subsubsection{Reward Performance: A Trade-off}

Despite preventing stopping, LayerNorm does not uniformly improve learning quality. Table~\ref{tab:layernorm_rewards} compares average rewards over the final 10 episodes for each configuration.

\begin{table}[htbp]
\caption{Final Reward Comparison: Baseline vs LayerNorm (Last 10-Episode Average)}
\label{tab:layernorm_rewards}
\centering
\small
\begin{tabular}{ccrrr}
\toprule
\textbf{Actor LR} & \textbf{Critic LR} & \textbf{Baseline} & \textbf{LayerNorm} & \textbf{$\Delta$} \\
\midrule
0.1 & 0.1 & $-$45,880 & $-$37,185 & +8,695 \\
0.1 & 0.01 & $-$37,052 & $-$33,998 & +3,054 \\
0.1 & 0.001 & $-$46,853 & $-$46,506 & +347 \\
0.1 & 0.0001 & $-$45,690 & $-$49,027 & $-$3,337 \\
0.01 & 0.1 & $-$44,277 & $-$52,977 & $-$8,700 \\
0.01 & 0.01 & $-$35,381 & $-$43,300 & $-$7,919 \\
0.01 & 0.001 & $-$47,863 & $-$51,642 & $-$3,779 \\
0.01 & 0.0001 & $-$51,333 & $-$43,525 & +7,808 \\
0.001 & 0.1 & $-$42,854 & $-$32,882 & +9,972 \\
0.001 & 0.01 & $-$42,954 & $-$26,094 & +16,860 \\
0.001 & 0.001 & $-$46,148 & $-$39,131 & +7,017 \\
0.001 & 0.0001 & $-$47,920 & $-$45,754 & +2,166 \\
0.0001 & 0.1 & $-$35,654 & $-$46,820 & $-$11,166 \\
0.0001 & 0.01 & $-$29,325 & $-$44,747 & $-$15,422 \\
0.0001 & 0.001 & $-$35,812 & $-$28,768 & +7,044 \\
0.0001 & 0.0001 & $-$32,547 & $-$44,934 & $-$12,387 \\
\bottomrule
\end{tabular}
\end{table}

The reward comparison reveals a clear pattern:
\begin{itemize}
    \item \textbf{Actor LR = 0.001:} LayerNorm improves rewards across all four critic LRs, with the best improvement at critic LR = 0.01 ($-$42,954 $\rightarrow$ $-$26,094). This is the best-performing LayerNorm configuration overall.

    \item \textbf{Actor LR = 0.1:} Mixed results---rewards improve for some critic LRs where baseline stopped early, but not consistently.

    \item \textbf{Actor LR = 0.0001:} LayerNorm \textit{substantially degrades} performance. The baseline's best configuration (actor LR = 0.0001, critic LR = 0.01, reward $-$29,325) drops to $-$44,747 with LayerNorm---a 1.5$\times$ deterioration. LayerNorm hurts configurations that already work well in the low-LR regime.
\end{itemize}

\subsubsection{Saturation Persists Despite Normalization}

Unexpectedly, output saturation ratios remain high even with LayerNorm. For actor LR $\geq$ 0.01, final saturation reaches 0.97--1.0, and even conservative actor LRs (0.0001) show saturation ratios of 0.96--0.97. Pre-activation values before the LayerNorm layer still reach $\pm 10^6$ for actor LR = 0.1, indicating that LayerNorm constrains the normalized output but cannot prevent the underlying weight explosion.

The key distinction is that LayerNorm maintains gradient flow through the normalization parameters even when the tanh output is saturated. This explains why actors continue updating (no stopping) despite high saturation: the LayerNorm layer provides an alternative gradient path that bypasses the near-zero tanh derivative.

\subsubsection{Interpretation}

LayerNorm presents a trade-off rather than a solution:
\begin{itemize}
    \item \textbf{Prevents stopping:} 13/16 vs 6/16 configurations complete full training---a significant improvement for robustness across hyperparameter choices
    \item \textbf{Shifts the optimal operating point:} The best actor LR shifts from 0.0001 (baseline, reward $-$29,325) to 0.001 (LayerNorm, reward $-$26,094). While LayerNorm's best reward slightly outperforms the baseline's best, this improvement comes at the cost of degrading the low-LR configurations where the baseline naturally succeeds
    \item \textbf{Degrades low-LR configurations:} Configurations with actor LR = 0.0001 that work well without normalization are substantially worse with LayerNorm, suggesting that the normalization interferes with the actor's ability to learn precise policies in the conservative-LR regime
\end{itemize}

This result suggests that LayerNorm alone is insufficient as a mitigation for gradient asymmetry. While it addresses the \textit{symptom} (actor stopping), it does not resolve the \textit{underlying cause} (the fundamental gradient magnitude imbalance between actor and critic). More targeted approaches---such as Squashed Gaussian parameterization or reward normalization combined with LayerNorm---may be necessary to achieve both robustness and performance.

\subsection{Input Normalization Eliminates Detected Stopping but Not Saturation (Preliminary)}

\textit{Note: The following experiments are ongoing. Results presented here are preliminary and subject to revision.}

An alternative normalization strategy is to apply normalization at the network \textit{input} rather than before the output activation. We tested this by adding a RunningNorm layer---which normalizes inputs using exponential moving averages of population mean and variance---to both actor and critic network inputs. Unlike LayerNorm (which normalizes across features per-sample), RunningNorm tracks cross-sample statistics, making it more suitable for the non-stationary data distributions common in RL. Crucially, \textbf{no normalization is applied to hidden layers or before the tanh output}.

\textbf{Architecture:}
\begin{itemize}
    \item Actor: RunningNorm $\rightarrow$ 64 $\rightarrow$ ReLU $\rightarrow$ 32 $\rightarrow$ ReLU $\rightarrow$ tanh $\rightarrow$ 3
    \item Critic: RunningNorm $\rightarrow$ 512 $\rightarrow$ ReLU $\rightarrow$ 128 $\rightarrow$ ReLU $\rightarrow$ 1 (linear)
\end{itemize}

\subsubsection{Stopping Behavior: Complete Elimination}

InputNorm achieves the strongest stopping prevention of any mitigation tested: \textbf{all 16 of 16 configurations} show zero actors stopped, compared to 6/16 (baseline), 8/16 (gradient clipping), and 13/16 (LayerNorm). Table~\ref{tab:inputnorm_stopping} compares stopping episodes.

\begin{table}[htbp]
\caption{Stopping Episodes: Baseline vs InputNorm}
\label{tab:inputnorm_stopping}
\centering
\small
\begin{tabular}{ccrr}
\toprule
\textbf{Actor LR} & \textbf{Critic LR} & \textbf{Baseline} & \textbf{InputNorm} \\
\midrule
0.1 & 0.1 & 161 & \textbf{2000}$^\dagger$ \\
0.1 & 0.01 & 162 & \textbf{2000} \\
0.1 & 0.001 & 222 & \textbf{2000} \\
0.1 & 0.0001 & 213 & \textbf{2000} \\
0.01 & 0.1 & 260 & \textbf{2000} \\
0.01 & 0.01 & 253 & \textbf{2000} \\
0.01 & 0.001 & 259 & \textbf{2000} \\
0.01 & 0.0001 & 260 & \textbf{2000} \\
0.001 & 0.1 & 944 & \textbf{2000} \\
0.001 & 0.01 & 2000 & 2000 \\
0.001 & 0.001 & 2000 & 2000 \\
0.001 & 0.0001 & 2000 & 2000 \\
0.0001 & 0.1 & 658 & \textbf{2000} \\
0.0001 & 0.01 & 2000 & 2000 \\
0.0001 & 0.001 & 2000 & 2000 \\
0.0001 & 0.0001 & 2000 & 2000 \\
\bottomrule
\multicolumn{4}{l}{\footnotesize $^\dagger$1,900 episodes completed (session timeout); 0/50 actors stopped.}
\end{tabular}
\end{table}

All ten configurations that exhibited stopping in the baseline now run to completion. No individual actor (out of 50 per configuration) stopped in any experiment. This is a substantially stronger result than LayerNorm, which still had three configurations with stopping and one regression.

\subsubsection{Reward Performance: Significant Degradation}

Despite eliminating detected stopping, InputNorm does not improve---and in most cases substantially \textit{degrades}---reward performance. Table~\ref{tab:inputnorm_rewards} compares last-10-episode average rewards.

\begin{table}[htbp]
\caption{Final Reward Comparison: Baseline vs InputNorm (Last 10-Episode Average)}
\label{tab:inputnorm_rewards}
\centering
\small
\begin{tabular}{ccrrr}
\toprule
\textbf{Actor LR} & \textbf{Critic LR} & \textbf{Baseline} & \textbf{InputNorm} & \textbf{$\Delta$} \\
\midrule
0.1 & 0.1 & $-$45,880 & $-$44,990 & +890 \\
0.1 & 0.01 & $-$37,052 & $-$42,402 & $-$5,350 \\
0.1 & 0.001 & $-$46,853 & $-$57,561 & $-$10,708 \\
0.1 & 0.0001 & $-$45,690 & $-$59,747 & $-$14,057 \\
0.01 & 0.1 & $-$44,277 & $-$60,814 & $-$16,537 \\
0.01 & 0.01 & $-$35,381 & $-$33,978 & +1,403 \\
0.01 & 0.001 & $-$47,863 & $-$44,698 & +3,165 \\
0.01 & 0.0001 & $-$51,333 & $-$45,021 & +6,312 \\
0.001 & 0.1 & $-$42,854 & $-$59,331 & $-$16,477 \\
0.001 & 0.01 & $-$42,954 & $-$59,798 & $-$16,844 \\
0.001 & 0.001 & $-$46,148 & $-$59,645 & $-$13,497 \\
0.001 & 0.0001 & $-$47,920 & $-$57,342 & $-$9,422 \\
0.0001 & 0.1 & $-$35,654 & $-$58,473 & $-$22,819 \\
0.0001 & 0.01 & $-$29,325 & $-$54,150 & $-$24,825 \\
0.0001 & 0.001 & $-$35,812 & $-$68,231 & $-$32,419 \\
0.0001 & 0.0001 & $-$32,547 & $-$44,047 & $-$11,500 \\
\bottomrule
\end{tabular}
\end{table}

The reward comparison reveals widespread performance degradation:
\begin{itemize}
    \item \textbf{13 of 16 configurations are worse} with InputNorm, with degradations up to $-$32,419 (actor LR = 0.0001, critic LR = 0.001).

    \item \textbf{Low actor LR configurations are most affected:} Actor LR = 0.0001 and 0.001 show uniformly worse rewards across all critic LRs. The baseline's best configuration (actor LR = 0.0001, critic LR = 0.01, reward $-$29,325) drops to $-$54,150 with InputNorm---a 1.8$\times$ deterioration.

    \item \textbf{Only actor LR = 0.01 shows mild improvement:} Three of four critic LR combinations improve slightly, with the best at critic LR = 0.0001 (+6,312). The best InputNorm reward overall ($-$33,978 at actor LR = 0.01, critic LR = 0.01) is still worse than the baseline's best ($-$29,325).
\end{itemize}

\subsubsection{Saturation Analysis: Complete Saturation Despite No Detected Stopping}

The most revealing finding is that \textbf{output saturation reaches 100\% across all 16 configurations}---every actor output is saturated ($|\tanh(z)| > 0.9$) by the end of training. Pre-activation magnitudes confirm:

\begin{table}[htbp]
\caption{InputNorm Final Pre-activation Magnitudes}
\label{tab:inputnorm_preact}
\centering
\small
\begin{tabular}{ccc}
\toprule
\textbf{Actor LR} & \textbf{Pre-activation Range} & \textbf{Saturation Ratio} \\
\midrule
0.1 & $\pm$1,374 to $\pm$3,442 & 1.00 \\
0.01 & $\pm$109 to $\pm$199 & 1.00 \\
0.001 & $\pm$21 to $\pm$37 & 1.00 \\
0.0001 & $\pm$12 to $\pm$17 & 1.00 \\
\bottomrule
\end{tabular}
\end{table}

Compared to the baseline (Table~\ref{tab:preactivation}), InputNorm reduces pre-activation magnitudes by roughly 20--50$\times$ for high actor learning rates (e.g., $\pm$1,800 vs $\pm$66,600 at actor LR = 0.1). However, even the reduced values are far above the threshold where tanh gradients vanish ($|z| > 5$ yields $\tanh'(z) < 0.0002$), meaning gradient flow through the tanh output layer is effectively zero in all configurations.

\subsubsection{Interpretation: Running Statistics Mask Stopping Detection}

The apparent contradiction---zero detected stopping despite 100\% saturation and poor rewards---has a specific technical explanation. RunningNorm maintains \texttt{running\_mean}, \texttt{running\_var}, and \texttt{count} as registered buffers in the network's state dictionary. These statistics update on every forward pass during training (via exponential moving average), regardless of whether gradient-based weight updates occur.

Our stopping detection checks whether \textit{any} parameter in the actor's state dictionary has changed between episodes. Because RunningNorm buffers are included in the state dictionary and change continuously, the detection \textbf{never triggers}---even if the learned parameters (fc1, fc2, fc3 weights and biases) have effectively frozen due to vanishing tanh gradients.

This creates a misleading picture:
\begin{itemize}
    \item The actor's state dictionary changes every episode (running statistics update)
    \item But the learned parameters may not be receiving meaningful gradient updates
    \item The actor's effective function shifts due to normalization drift, not policy learning
    \item This normalization drift does not constitute useful policy improvement, as confirmed by the poor reward performance
\end{itemize}

\subsubsection{Comparison with LayerNorm}

The contrast between InputNorm and LayerNorm is instructive:

\begin{itemize}
    \item \textbf{LayerNorm} (before tanh output): Provides an \textit{alternative gradient path} through the normalization parameters, enabling genuine weight updates even when tanh is saturated. Result: 13/16 no-stop with improved rewards at actor LR = 0.001.

    \item \textbf{InputNorm} (at network input): Normalizes input scale but does not address the tanh gradient bottleneck. Running statistics create an \textit{artifact in stopping detection} rather than maintaining gradient flow. Result: 16/16 apparent no-stop, but with universally worse rewards.
\end{itemize}

This comparison reinforces that the critical intervention point for mitigating tanh saturation is \textit{at or near the output layer}, not at the input. Input normalization is beneficial for training stability in general, but it cannot substitute for addressing the saturating activation directly.

\subsection{Adaptive Scaling Maintains Gradient Flow and Improves Performance (Preliminary)}

\textit{Note: The following experiments are ongoing. Results presented here are preliminary and subject to revision.}

Rather than normalizing inputs or constraining pre-activations, adaptive scaling addresses saturation at the output layer by dynamically rescaling pre-activations \textit{before} the tanh function. The approach tracks strict per-dimension min/max of raw pre-activations ($G_\text{min}$, $G_\text{max}$) and linearly maps the observed range to a fixed target interval $[-r, +r]$ where $r = 2$:

\begin{equation}
z_\text{scaled} = \frac{z - \bar{G}}{(G_\text{max} - G_\text{min})/2} \cdot r, \quad \bar{G} = \frac{G_\text{max} + G_\text{min}}{2}
\label{eq:adaptive_scaling}
\end{equation}

The tanh activation then receives $z_\text{scaled} \in [-2, +2]$, where $\tanh(\pm 2) \approx \pm 0.964$---utilizing most of the output range while maintaining viable gradients with $\tanh'(\pm 2) \approx 0.07$. Tracking is strict (only expands, never shrinks) and per-dimension, requiring no hyperparameter tuning beyond the target range. Additionally, gradient clipping (max\_norm = 1.0) is applied to both actor and critic networks. Only the actor uses adaptive scaling; the critic retains its original architecture with linear output.

\subsubsection{Stopping Behavior: Complete Elimination}

Adaptive scaling achieves \textbf{all 16 of 16 configurations} without stopping, compared to 6/16 in the baseline. Table~\ref{tab:adaptive_stopping} compares stopping episodes.

\begin{table}[htbp]
\caption{Stopping Episodes: Baseline vs Adaptive Scaling}
\label{tab:adaptive_stopping}
\centering
\small
\begin{tabular}{ccrr}
\toprule
\textbf{Actor LR} & \textbf{Critic LR} & \textbf{Baseline} & \textbf{Adaptive} \\
\midrule
0.1 & 0.1 & 161 & \textbf{2000} \\
0.1 & 0.01 & 162 & \textbf{2000} \\
0.1 & 0.001 & 222 & \textbf{2000} \\
0.1 & 0.0001 & 213 & \textbf{2000} \\
0.01 & 0.1 & 260 & \textbf{2000} \\
0.01 & 0.01 & 253 & \textbf{2000} \\
0.01 & 0.001 & 259 & \textbf{2000} \\
0.01 & 0.0001 & 260 & \textbf{2000} \\
0.001 & 0.1 & 944 & \textbf{2000} \\
0.001 & 0.01 & 2000 & 2000 \\
0.001 & 0.001 & 2000 & 2000 \\
0.001 & 0.0001 & 2000 & 2000 \\
0.0001 & 0.1 & 658 & \textbf{2000} \\
0.0001 & 0.01 & 2000 & 2000 \\
0.0001 & 0.001 & 2000 & 2000 \\
0.0001 & 0.0001 & 2000 & 2000 \\
\bottomrule
\end{tabular}
\end{table}

Adaptive scaling with $r = 2$ and gradient clipping \textbf{completely eliminates stopping} across all 16 configurations. All ten configurations that previously stopped in the baseline (at episodes 161--944) now complete the full 2,000 episodes, including the four critic LR = 0.1 configurations that remained problematic under other mitigations (gradient clipping, LayerNorm). The combination of bounded pre-activations and gradient clipping proves sufficient to stabilize training even at the most extreme learning rate combinations.

\textbf{Note on stopping detection:} Like InputNorm, the adaptive scaling actor includes registered buffers ($G_\text{min}$, $G_\text{max}$) in the state dictionary that could affect stopping detection. However, unlike InputNorm's running statistics (which change every forward pass), the strict min/max buffers only expand when \textit{new} pre-activation extremes are observed. In all 16 configurations, the continued expansion of $G_\text{min}$/$G_\text{max}$ is consistent with ongoing weight updates that produce new pre-activation extremes, confirming that the actors are genuinely learning rather than merely appearing active due to buffer changes.

\subsubsection{Gradient Flow: How Adaptive Scaling Prevents Vanishing}

The key mechanism is that adaptive scaling bounds the tanh input to $[-2, +2]$, ensuring a minimum gradient magnitude. The gradient of the actor output with respect to the raw pre-activation $z$ is:

\begin{equation}
\frac{\partial \tanh(z_\text{scaled})}{\partial z} = \tanh'(z_\text{scaled}) \cdot \frac{r}{(G_\text{max} - G_\text{min})/2}
\label{eq:adaptive_gradient}
\end{equation}

For values near the center of the distribution, $z_\text{scaled} \approx 0$ and $\tanh'(0) = 1$, yielding a gradient of $2r / (G_\text{max} - G_\text{min})$. Even at the extremes, $\tanh'(\pm 2) \approx 0.07$ provides a substantial gradient---two orders of magnitude larger than $\tanh'(\pm 4) \approx 0.0013$. In contrast, the baseline produces pre-activations of $\pm 10^6$ where $\tanh'(z) \approx 0$ to machine precision.

The trade-off is that as pre-activations grow and $(G_\text{max} - G_\text{min})$ expands, the scaling factor $r / ((G_\text{max} - G_\text{min})/2)$ shrinks, attenuating gradients. However, this attenuation is gradual (proportional to $1/G_\text{range}$) rather than catastrophic (exponentially decaying to zero as in unscaled tanh). Combined with gradient clipping (max\_norm = 1.0) that prevents explosive updates, this maintains sufficient gradient flow for all 16 configurations to continue learning throughout training.

\subsubsection{Saturation Analysis}

A methodological note: our tracking infrastructure records saturation from \textit{raw} pre-activations (fc3 output before scaling), computing $|\tanh(z_\text{raw})| > 0.9$. Since $z_\text{raw}$ grows to extreme values, the reported saturation ratio approaches 1.0 for all configurations. However, the \textit{actual} network output is $\tanh(z_\text{scaled})$ where $z_\text{scaled} \in [-2, +2]$. The effective saturation---the fraction of outputs where the gradient is significantly attenuated---is determined by the distribution of $z_\text{scaled}$, not $z_\text{raw}$.

Raw pre-activation magnitudes grow substantially over training, confirming that the actor weights continue to evolve:

\begin{table}[htbp]
\caption{Adaptive Scaling: Raw Pre-activation Growth Over Training}
\label{tab:adaptive_preact}
\centering
\small
\begin{tabular}{cccc}
\toprule
\textbf{Actor LR} & \textbf{Early (ep 10)} & \textbf{Mid (ep 1000)} & \textbf{Late (ep 2000)} \\
\midrule
0.1 & $\pm 5.8 \times 10^4$ & $\pm 2.6 \times 10^{10}$ & $\pm 1.4 \times 10^{11}$ \\
0.01 & $\pm 262$ & $\pm 6.0 \times 10^7$ & $\pm 5.3 \times 10^8$ \\
0.001 & $\pm 11$ & $\pm 5.3 \times 10^5$ & $\pm 3.3 \times 10^6$ \\
0.0001 & $\pm 2.3$ & $\pm 2.8 \times 10^3$ & $\pm 1.5 \times 10^4$ \\
\bottomrule
\end{tabular}
\end{table}

The continued growth of raw pre-activations across all 16 configurations confirms that the learned parameters are genuinely updating---if weights were frozen, pre-activations would be bounded by the finite state space and would plateau.

\subsubsection{Reward Performance: Significant Improvement for High Actor LR}

Unlike InputNorm and LayerNorm, adaptive scaling shows substantial reward improvements for configurations that previously failed. Table~\ref{tab:adaptive_rewards} compares last-10-episode average rewards.

\begin{table}[htbp]
\caption{Final Reward Comparison: Baseline vs Adaptive Scaling (Last 10-Episode Average)}
\label{tab:adaptive_rewards}
\centering
\small
\begin{tabular}{ccrrr}
\toprule
\textbf{Actor LR} & \textbf{Critic LR} & \textbf{Baseline} & \textbf{Adaptive} & \textbf{$\Delta$} \\
\midrule
0.1 & 0.1 & $-$45,880 & $-$35,174 & +10,706 \\
0.1 & 0.01 & $-$37,052 & $-$28,989 & +8,063 \\
0.1 & 0.001 & $-$46,853 & $-$35,295 & +11,558 \\
0.1 & 0.0001 & $-$45,690 & $-$34,811 & +10,879 \\
0.01 & 0.1 & $-$44,277 & $-$46,055 & $-$1,778 \\
0.01 & 0.01 & $-$35,381 & $-$38,041 & $-$2,660 \\
0.01 & 0.001 & $-$47,863 & $-$32,027 & +15,836 \\
0.01 & 0.0001 & $-$51,333 & $-$33,051 & +18,282 \\
0.001 & 0.1 & $-$42,854 & $-$36,688 & +6,166 \\
0.001 & 0.01 & $-$42,954 & $-$28,748 & +14,206 \\
0.001 & 0.001 & $-$46,148 & $-$38,133 & +8,015 \\
0.001 & 0.0001 & $-$47,920 & $-$28,168 & +19,752 \\
0.0001 & 0.1 & $-$35,654 & $-$35,927 & $-$273 \\
0.0001 & 0.01 & $-$29,325 & $-$36,866 & $-$7,541 \\
0.0001 & 0.001 & $-$35,812 & $-$42,054 & $-$6,242 \\
0.0001 & 0.0001 & $-$32,547 & $-$41,759 & $-$9,212 \\
\bottomrule
\end{tabular}
\end{table}

The reward comparison reveals a clear pattern tied to actor learning rate:
\begin{itemize}
    \item \textbf{Actor LR = 0.1:} All four configurations show large improvements (+8,063 to +11,558), as actors that previously stopped within 200 episodes now train for the full 2,000. The best reward ($-$28,989 at critic LR = 0.01) surpasses the baseline's overall best ($-$29,325).

    \item \textbf{Actor LR = 0.01:} Two of four configurations improve substantially (up to +18,282 at critic LR = 0.0001), while two slightly degrade. The improvements come from configurations that stopped at $\sim$260 episodes in the baseline.

    \item \textbf{Actor LR = 0.001:} All four configurations improve, with the largest gain at critic LR = 0.0001 (+19,752), achieving the best single-configuration reward of $-$28,168.

    \item \textbf{Actor LR = 0.0001:} All four configurations degrade, with the baseline's best (critic LR = 0.01, $-$29,325) dropping to $-$36,866. The adaptive scaling mechanism appears to interfere with the fine-grained updates that characterize successful low-LR training.
\end{itemize}

Overall, \textbf{10 of 16 configurations improve} with adaptive scaling---the highest ratio of any mitigation tested. The best adaptive reward ($-$28,168) \textit{surpasses} the baseline's best ($-$29,325), while the number of competitive configurations ($<$ $-$40,000) increases from 6 in the baseline to 13 with adaptive scaling.

\subsubsection{Interpretation}

Adaptive scaling is the most effective mitigation strategy tested in this work:
\begin{itemize}
    \item \textbf{Complete stopping prevention:} All 16/16 configurations avoid stopping---the only mitigation besides InputNorm to achieve this, and without the detection artifacts that plague InputNorm. The combination of bounded pre-activations ($r = 2$) and gradient clipping resolves even the critic LR = 0.1 failure mode that persisted under all other mitigations.

    \item \textbf{Genuine learning:} Unlike InputNorm (which masked stopping detection through buffer drift), adaptive scaling maintains actual gradient flow through the tanh activation. The continued growth of raw pre-activations and the substantial reward improvements confirm that actors are genuinely learning.

    \item \textbf{Reward improvement:} 10/16 configurations show better rewards than baseline, with the largest gains in previously-failed high-LR configurations. The best adaptive reward ($-$28,168) surpasses the baseline's best ($-$29,325).

    \item \textbf{Low-LR degradation:} Like LayerNorm, adaptive scaling degrades the actor LR = 0.0001 configurations that already work well in the baseline. The scaling mechanism, designed to prevent saturation at high learning rates, introduces unnecessary perturbation at low learning rates where saturation is not the binding constraint.
\end{itemize}

The success of adaptive scaling---which directly addresses the tanh output activation---further confirms the central thesis of this paper: the gradient asymmetry problem is rooted in the tanh output layer, and effective mitigations must operate at or near this activation. Input normalization (wrong location) and increased capacity (wrong mechanism) are insufficient; interventions that bound or rescale the tanh input (adaptive scaling, LayerNorm) produce meaningful improvements.

\subsection{Comparative Summary of Mitigation Strategies}
\label{subsec:mitigation_summary}

Table~\ref{tab:mitigation_summary} consolidates results across all six experimental conditions.

\begin{table}[htbp]
\caption{Comparative Summary of All Mitigation Experiments}
\label{tab:mitigation_summary}
\centering
\small
\begin{tabular}{lccccc}
\toprule
\textbf{Experiment} & \textbf{No-Stop} & \textbf{Configs} & \textbf{Best} & \textbf{Mechanism} \\
 & \textbf{Configs} & \textbf{Improved$^\dagger$} & \textbf{Reward} & \\
\midrule
Baseline & 6/16 & --- & $-$29,325 & --- \\
Large Actor & 3/16 & 1/16 & $-$29,363 & Capacity \\
Grad.\ Clipping & 8/16 & 4/16 & $-$29,399 & Gradient limit \\
LayerNorm & 13/16 & 8/16 & $-$26,094 & Output norm. \\
InputNorm & 16/16$^*$ & 3/16 & $-$33,978 & Input norm. \\
Adaptive Scaling & 16/16 & 10/16 & $-$28,168 & Output scaling \\
\bottomrule
\multicolumn{5}{l}{\footnotesize $^\dagger$Configs with better last-10-episode reward than baseline.} \\
\multicolumn{5}{l}{\footnotesize $^*$Detection artifact; see Section~6.10.4.}
\end{tabular}
\end{table}

Several patterns emerge from this comparison:

\textbf{Convergence rate, not stopping rate, is the critical metric.} The ``Configs Improved'' column in Table~\ref{tab:mitigation_summary} is more informative than ``No-Stop Configs'' for evaluating mitigation effectiveness. InputNorm achieves 16/16 no-stop but the lowest convergence improvement (3/16 configs with better reward). In contrast, adaptive scaling achieves both 16/16 no-stop \textit{and} the highest convergence rate (10/16 configs improved). This demonstrates that \textit{preventing stopping is necessary but not sufficient}---the mitigation must also enable the actor to learn a better policy.

\textbf{Location matters more than mechanism.} The two most effective mitigations---LayerNorm and adaptive scaling---both operate at or near the tanh output layer. LayerNorm normalizes pre-activations; adaptive scaling rescales them. Both maintain gradient flow through the critical bottleneck. In contrast, InputNorm (at the network input) and large actor (distributed capacity) fail to address the actual gradient vanishing point, producing either detection artifacts or worse performance.

\textbf{Critic LR = 0.1 is nearly universally problematic.} Across most mitigations, configurations with critic LR = 0.1 remain the most likely to exhibit stopping or degraded performance. Adaptive scaling is the notable exception: by combining bounded pre-activations with gradient clipping on both actor and critic, it fully resolves the critic LR = 0.1 stopping failure. However, reward performance at critic LR = 0.1 remains mixed, suggesting that extreme critic learning rates create optimization challenges beyond stopping alone.

\textbf{Low actor LR configurations degrade.} Both LayerNorm and adaptive scaling improve high-LR configurations at the cost of degrading low-LR ones (actor LR = 0.0001). The baseline's best configuration (actor LR = 0.0001, critic LR = 0.01, reward $-$29,325) worsens with every mitigation except gradient clipping, which leaves it unchanged. This suggests that the mitigations introduce perturbation that disrupts the fine-grained learning that already succeeds in the conservative-LR regime.

\textbf{Adaptive scaling is the most complete mitigation.} Adaptive scaling achieves the best overall profile: 16/16 no-stop, 10/16 improved reward, and a best reward ($-$28,168) that surpasses the baseline's best ($-$29,325). LayerNorm achieves a better single-configuration reward ($-$26,094) but with fewer no-stop configurations (13/16). An optimal strategy might combine approaches---for example, using adaptive scaling for high actor learning rates while leaving low-LR configurations unmodified.
